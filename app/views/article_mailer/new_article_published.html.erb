<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      background-color: black;
      font-family: Georgia, 'Times New Roman', serif;
      margin: 0;
      padding: 20px;
    }
    
    .email-container {
      background-color: #e4daca;
      max-width: 600px;
      margin: 0 auto;
      padding: 2em;
    }
    
    .masthead {
      font-family: Georgia, serif;
      font-size: 3em;
      font-weight: bold;
      text-align: center;
      color: black;
      margin-bottom: 0.5em;
      letter-spacing: 0.05em;
    }
    
    .subtitle {
      font-family: Georgia, serif;
      font-size: 1.5em;
      text-align: center;
      color: black;
      margin-bottom: 2em;
      font-style: italic;
    }
    
    .article-title {
      text-align: center;
      font-family: Georgia, serif;
      font-weight: 700;
      font-size: 1.8em;
      color: black;
      margin-bottom: 0.5em;
    }
    
    .article-byline {
      text-align: center;
      font-family: Georgia, serif;
      font-weight: 500;
      font-size: 1em;
      color: #333;
      margin-bottom: 1.5em;
    }
    
    .article-thumbnail {
      width: 100%;
      max-width: 500px;
      display: block;
      margin: 1.5em auto;
      border: solid 2px #0000001a;
      border-radius: 20px;
    }
    
    .article-blurb {
      font-family: Georgia, serif;
      font-size: 1.1em;
      line-height: 1.6;
      color: black;
      margin-bottom: 2em;
      text-align: left;
    }
    
    .read-more-button {
      display: block;
      width: 80%;
      max-width: 400px;
      margin: 2em auto;
      padding: 1em;
      text-align: center;
      font-family: Georgia, serif;
      font-size: 1.2em;
      font-weight: 700;
      background-color: #000;
      color: #e4daca;
      text-decoration: none;
      border: 2px solid #000;
      border-radius: 10px;
    }
    
    .read-more-button:hover {
      background-color: #e4daca;
      color: #000;
    }
    
    .footer {
      text-align: center;
      font-size: 0.9em;
      color: #666;
      margin-top: 2em;
      padding-top: 1em;
      border-top: 1px solid #0000001a;
    }
  </style>
</head>
<body>
  <div class="email-container">
    <div class="masthead">JUMP News</div>
    <div class="subtitle"><%= Article.random_motto %></div>
    
    <h1 class="article-title"><%= @article.title %></h1>
    <div class="article-byline">By <%= @article.byline %> - <%= @article.fake_created_at %></div>
    
    <% if @article.thumbnail.present? %>
      <img src="<%= root_url.chomp('/') + asset_path(@article.thumbnail) %>" class="article-thumbnail" alt="<%= @article.title %>">
    <% end %>
    
    <div class="article-blurb">
      <%= raw(@blurb.gsub(/<\/?[^>]*>/, '')) %>
    </div>
    
    <%= link_to 'Get the Full Scoop', article_url(@article), class: 'read-more-button' %>
  </div>
</body>
</html>
